import React from 'react';

import { Menu, Settings, Theme } from '../components';

export default {
  title: 'LeftHeader',
};

export const MenuContext = () => <Menu />;
export const SettingsContext = () => <Settings />;
export const ThemeChose = () => <Theme />;
